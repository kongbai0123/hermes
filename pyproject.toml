[project]
name = "hermes-agent-os"
version = "0.1.0"
description = "An evolving execution system for local AI agents."
authors = [{name = "Antigravity"}]
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = "test_*.py"
